# Agent instructions

This is a `{{ project_kind }}` operating baseline generated at template revision `{{ template_revision }}`.

- Use Beads as the canonical durable backlog.
- Use SpecKit for consequential behavior and automation contracts.
- Keep credentials out of source, logs, and URLs.
- Do not start, stop, restart, or reconfigure the shared Dolt service from this repository.
- Do not treat this operating baseline as application implementation.

Before completion, run applicable checks, commit atomically using Conventional Commits, and verify Git and Beads/Dolt synchronization independently.
